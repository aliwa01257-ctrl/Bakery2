# مخبز البيت - تطبيق طلب المخبوزات (Streamlit)

هذا المشروع عبارة عن تطبيق ويب بسيط لعرض وطلب المخبوزات بدون قاعدة بيانات، مبني باستخدام:

- Python
- Streamlit
- CSS مخصص (ملف `style.css`)
- إرسال الطلبات عبر واتساب فقط

## تشغيل المشروع محلياً

1. إنشاء بيئة عمل (اختياري لكن مستحسن)
2. تثبيت المتطلبات:

```bash
pip install -r requirements.txt
